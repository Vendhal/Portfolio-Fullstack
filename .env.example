# Database connection used by Spring Boot and PostgreSQL
DB_HOST=db
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=Sunnysandy@1

# Ports exposed when using docker-compose.prod.yml
BACKEND_PORT=8080
FRONTEND_PORT=80

# Spring Boot profile
SPRING_PROFILES_ACTIVE=prod

# Optional: override the API URL baked into the frontend build
# VITE_API_BASE_URL=https://your-domain/api
# Host port exposed by docker-compose for pgAdmin or other tools
HOST_DB_PORT=5433

JWT_SECRET=dev-secret-change-me-1234567890123456
