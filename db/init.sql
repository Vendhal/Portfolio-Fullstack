-- Optional: create schema objects or set defaults.
-- With JPA ddl-auto=update, tables will be created automatically.
-- You can use this file to create users or seed data if desired.

-- Example: ensure timezone
SET GLOBAL time_zone = "+00:00";
